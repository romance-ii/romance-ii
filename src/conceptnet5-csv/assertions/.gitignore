# The CSV files are too large for GitHub,
# so we only check in their BZ2 forms. (I know, crazy/backwards)
*.csv

# Also, the DB file is huge and generated from the above.
# So it can be omitted as well.

conceptnet5.sqlite.db

